@model TEST.Models.DICHVU

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="wrapper">
        <h2 style="text-align:center">XÓA THÔNG TIN</h2>
        <div class="form">
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.MADV)

            <div class="form-group">
                <label>@Html.DisplayNameFor(model => model.TENDV)</label>
                <p class="form-control">@Html.DisplayFor(model => model.TENDV)</p>
            </div>

            <div class="form-group">
                <label>@Html.DisplayNameFor(model => model.DONGIADV)</label>
                <p class="form-control">@Html.DisplayFor(model => model.DONGIADV)</p>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <h4>Chắc chắn xóa thông tin này ?</h4>
                    <input type="submit" value="Xóa" class="btn" />
                </div>
            </div>

            <div style="text-align:center">
                @Html.ActionLink("Trở Lại", "Index")
            </div>
        </div>
    </div>

}

@using TEST.Models
@{
    ViewBag.Title = "GioiThieu";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";

    <style>
        #bgimg {
            position: absolute;
            top: 40px;
            right: -100px;
            width: 650px;
            z-index: -1;
        }

        #gth {
            position: absolute;
            top: 60px;
            left: -40px;
            width: 550px;
        }
    </style>
    QLBNKMEntities db = new QLBNKMEntities();
    TAIKHOAN tk;
}

<p style="font-weight: 700;
        font-size: 20px;
        color: #14B3C6;
        margin-top: 30px;
        margin-left: -20px;">
    Trưởng khoa:
    @{
        tk = null;
        if (Session["TaiKhoanAdmin"] != null) { tk = Session["TaiKhoanAdmin"] as TAIKHOAN; }
        BACSI bs = db.BACSIs.SingleOrDefault(x => x.MABS == tk.MABS);
        <span>@bs.TENBS</span>
    }
</p>
    <img src="~/Images/anhbg2.jpg" id="bgimg" />
    <img src="~/Images/Picture1.png" id="gth" />


